# Generated / runtime data — scraped source, caches, rendered manga, ledger (NEVER commit:
# may contain copyrighted source text, character art, and generated pages).
data/

# Secrets & env
.env
.env.*

# Python / tooling
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
.mypy_cache/
.ruff_cache/

# Editors / OS
.idea/
.vscode/
.DS_Store
*.log

# Local working configs — your personal copies of the shipped templates, NOT committed
# (machine-specific, and may point at copyrighted reference images / URLs). See README §7.
#   config/run.yaml         = your copy of config/default.yaml          (with your overrides)
#   config/references.yaml  = your copy of config/references.example.yaml (character -> image map)
config/references.yaml
config/run.yaml
